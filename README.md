### Haskell OpenGL, multiple main 


* The project contains three OpenGL files:

```
helloWindow.hs
simpleCube.hs
rotateCube.hs

```

* Plz see the *.cabal

```
    -- build all projects
    stack build

    -- run it
    stack exec helloWindow
    stack exec simpleCube 
    stack exec rotateCube 
```
